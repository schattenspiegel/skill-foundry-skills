# ASGI verification

Test valid and malformed input at every location, missing versus null, extras,
aliases, unions, security-sensitive fields, response status/headers/body, and
OpenAPI schema. Override dependencies narrowly and restore them. Ensure response
validation defects are observed as server errors rather than relabeled 422s.
