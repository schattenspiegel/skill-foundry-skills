# Agent fields

Current VS Code fields include `name`, `description`, `argument-hint`, `tools`,
`agents`, `model`, `user-invocable`, `disable-model-invocation`, `target`, and
`handoffs`. `mcp-servers` is for the GitHub Copilot target rather than normal
VS Code agent configuration. Inspect the active schema before emitting fields.
