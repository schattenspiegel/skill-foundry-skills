# Verification

Parse YAML and type-check fields. Resolve tools, subagents, handoff targets, and
models against the active host. Test picker visibility and model invocation
separately. Snapshot files for read-only roles and inspect Agent Debug Logs.
