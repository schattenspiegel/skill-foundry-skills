# Verification

Parse JSON; reject credentials and ambiguous executable discovery; verify
transport-specific required fields, provenance, versions, and minimal scope.
Do not start the server during structural validation. After approval, inspect
trust, logs, exact tools, error behavior, and a non-mutating call.
