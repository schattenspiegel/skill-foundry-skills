# State and structure

The JSON notebook is not the running state. Kernels retain variables, imports,
open handles, warnings filters, and modified modules. Execution counts can
reveal disorder but are not complete proof. Inspect cells through nbformat,
preserve stable cell IDs, and minimize noisy metadata/output diffs.
