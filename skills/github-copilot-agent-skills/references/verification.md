# Verification

Validate metadata, word budget, references, file sizes, script syntax, and
runtime-only contents. Run explicit, implicit, baseline, adversarial, and
negative cases in isolation. Mock output proves plumbing only; inspect VS Code
slash discovery, implicit use, and resource access separately.
