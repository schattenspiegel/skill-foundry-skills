# Verification

Parse manifest/MCP/hook JSON and all YAML. Validate names, versions, schemas,
root tokens, paths, executable provenance, hashes, licenses, and absence of
secrets. Inspect package contents before install. After approval, test enable,
disable, update, uninstall, and loss of provided tools/commands when disabled.
